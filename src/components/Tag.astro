---
const {tag} = Astro.props;
---
<p class="rounded-xl p-2 m-0! hover:*:text-white! bg-slate-200 relative hover:bg-slate-800 transition">
  <a class="text-slate-600! transition" href={`/tags/${tag}`}>{tag}</a>
</p>

<style>
  a::after {
    content: none;
  }
  /* p::after {
    content: '';
    background-color: rgba(69, 148, 232, 0.223);
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2px;
    transition: height .4s ease-in;
    border-radius: var(--radius-xl);
    z-index: -1;
  }

  p:hover::after {
    height: 100%;
  } */
</style>